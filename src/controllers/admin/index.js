'use strict;';

const registerDeliveryGuy = require('./registerDeliveryGuy');
const makeAdmin = require('./makeAdmin');
const revokeAdmin = require('./revokeAdmin');

module.exports = {
  registerDeliveryGuy,
  makeAdmin,
  revokeAdmin,
};
